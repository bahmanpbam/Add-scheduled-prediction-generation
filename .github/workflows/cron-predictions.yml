name: Generate Predictions Cron

on:
  workflow_dispatch:      # allow manual runs
  schedule:
    - cron: "0 8 * * 1,3,5"   # Mon, Wed, Fri at 08:00 UTC

jobs:
  run-prediction-cron:
    runs-on: ubuntu-latest

    steps:
      - name: Call Supabase Function
        run: |
          curl -X POST \
            "https://iulictgvgodsmblhjgdy.functions.supabase.co/generate_predictions_cron"
